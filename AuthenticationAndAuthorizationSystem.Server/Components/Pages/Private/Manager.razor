@page "/manager"
@using Microsoft.AspNetCore.Authorization

@attribute [Authorize(Roles = $"{AuthenticationInitalData.SuperAdminRole},{AuthenticationInitalData.ManagerRole}")]

<PageTitle>Manager</PageTitle>

<h1>Manager page </h1>

<AuthorizeView>
    Hello @context.User.Identity?.Name!
</AuthorizeView>