@page "/marketing"

@using Microsoft.AspNetCore.Authorization

@attribute [Authorize(Roles = $"{AuthenticationInitalData.SuperAdminRole},{AuthenticationInitalData.MarketingRole}")]

<PageTitle>Marketing</PageTitle>

<h1>Marketing page </h1>

<AuthorizeView>
    Hello @context.User.Identity?.Name!
</AuthorizeView>